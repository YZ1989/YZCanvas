---
title: YZCanvas 待验收
description: 已实现的轻量版变更及人工验收边界
---

# YZCanvas 待验收

## 英伦复古工作台

- 保留左侧导航与底部账户布局，首页改为「光影有迹，想象无界。」与「在无限画布上，编织你的下一幕。」。
- 暖纸色与酒红色主视觉，摄影机、胶片、场记板静物插画；项目封面保留真实媒体或节点预览，增加胶片边缘。
- 新增图像资源合计约 144KB，标题字体字集约 12KB；字体与图片本地加载，无新运行时依赖。移除不再使用的旧工作台插画。
- 统一设置/账户/菜单、画布背景与工具面板配色，新增设备默认浅色；已有主题偏好继续保留，深色可切换。
- 浏览器验证 1431px 桌面、1000px 与 390px 窗口，搜索/清空、菜单/取消重命名、账户/设置、资产导航、新建画布/返回首页；298 项测试通过，2 项在线测试跳过。
- 请在原生 Windows 桌面验证标题栏配色与系统缩放 125%/150%；本轮仅更新源码和开发预览，没有打包，也没有执行付费生成。

## 左下角账户与登录

- 左下角账户入口：未连接时进入 API 设置；验证成功显示登录状态与钱包余额，可刷新或前往控制台。
- 设置复用 API Key 配置；保存前验证，失败不覆盖原密钥。退出清除所有派生渠道的 Key，保留画布和素材。
- 画布内使用紧凑账号按钮，避让缩放与左侧面板；顶栏移除重复配置入口，原 `/config` 路由保留。
- 已在浏览器验证现有密钥登录、真实余额、设置面板与画布布局；未执行充值或付费生成。新增无效密钥、零余额与退出清理测试。
- 请人工验收更换密钥、离线错误恢复和退出后重新登录；本次仅更新源码与开发预览。

- 快速启动：双击 `Start-YZCanvas.cmd` 自动打开浏览器测试版；重复启动复用服务，验证刷新保留浏览器内项目。桌面版数据独立，本次不打包。

## 模型目录更新（源码与开发预览）

- 恢复可灵、Vidu、Wan、HappyHorse 分组；保留未接入专项能力的禁用提示。
- 新增 G v2.5 Flare / Sunburst / Lowprice、Grok 2 多图编辑、Omni Flash / Omni 1.1 Flash 低价版。
- 验收质量、分辨率、输出数量、参考图限制与 Omni 参考视频模式；未调用付费生成。
- 本次不打包，现有安装包仍为上一次构建内容。

## 已完成自动与浏览器验证

- 类型检查与 281 项测试通过，2 项在线测试跳过。
- 浏览器实际验证中文文本、PNG 导入、1:1 裁剪、撤销重做、刷新后的文字和图片恢复。
- Windows Release NSIS 构建成功；体积记录见仓库 docs/measurements。

## 请在 Windows 桌面预览版验证

- 0.1.1 视觉更新：验证安装程序、桌面快捷方式、任务栏和窗口显示新 YZ 图标；启动页仅显示 YZCanvas，动画完成且数据就绪后进入画布；检查系统减少动态效果、深浅主题与启动失败兜底。

- Jinyu 接入：重新填写该站密钥，验证余额、图像生成、参考图编辑、视频生成与上传；旧密钥不得自动复用，设置页的控制台与文档链接应打开新域名。

- 命名统一：文档站、默认渠道、素材下载和配置导出使用 YZCanvas；检查浅色主题刷新后保持。已随 Jinyu 接入重新打包，桌面交互仍待验收。

- 安装与启动：标题显示 YZCanvas，深浅主题、窗口最小化/最大化/关闭正常；缺少 WebView2 的机器需要联网安装运行时。
- 本地数据：应用使用独立存储空间；重新启动后画布、图片与视频恢复，不覆盖原 TDCanvas 数据。
- 图片：导入、裁剪、绘制/编辑、下载和原生另存为；实际 API 生成、参考图编辑、重试与取消。
- 视频：导入、播放、保存、文生视频和图生视频；空闲节点仅预载元数据。
- 轻量范围：界面无 Agent、ComfyUI、插件管理和新建音频入口；旧项目音频仍可读取；未知扩展节点保留原始数据但不能执行。
- 项目导入导出：沿用 TDCanvas 格式，未知节点与连线能往返保存。
- 性能：100 张图片、多视频、大尺寸图片和长时间运行的内存与响应尚未测量。

当前 AI 已切换至 Jinyu API；真实付费生成未执行，不能将协议单元测试视为线上验收。完整桌面交互也未验收。
