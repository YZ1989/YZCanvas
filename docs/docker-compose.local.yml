services:
  docs:
    image: yzcanvas-docs:local
    container_name: yzcanvas-docs
    build:
      context: ..
      dockerfile: docs/Dockerfile
    ports:
      - "3001:3000"
    restart: unless-stopped
